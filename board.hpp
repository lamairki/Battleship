#ifndef         __H_BOARD__
#define         __H_BOARD__

const int BOARD_HEIGHT = 10;
const int BOARD_WIDTH = 10;

enum rowNames {
	A,
	B,
	C,
	D,
	E,
	F,
	G,
	H,
	I,
	J
};

/** This function will take in the vertical and
  * horizontal coordinates desired along with 
  * the cardinal direction the ship should
  * face and the ship, then adds it to the board.
**/



#endif